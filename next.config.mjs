/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.a.transfermarkt.technology', 'images.unsplash.com', 'www.apple.com'],
      },
};

export default nextConfig;
